#summary comboboxtext widget reference

= comboboxtext widget =
A [http://developer.gnome.org/gtk/2.24/GtkComboBox.html GtkComboBox]
----

== Definition ==
{{{
<comboboxtext tag_attr="value"...>
	<default>data</default>
	<variable>varname</variable>
	<input>command</input>
	<input file>filename</input>
	<visible>state</visible>
	<action>activity</action>...
	<action signal="event">activity</action>...
	<action type="function">parameter</action>...
	<item>string</item>...
	<output file>filename</output>
</button>
}}}
"..." denotes acceptance of multiples of the same thing.

== Tag Attributes ==
See the [http://developer.gnome.org/gtk/2.24/GtkComboBox.html#GtkComboBox.object-hierarchy GtkComboBox] widget and ancestor class properties.

The following custom tag attributes are available:
|| *Name* || *Description* || *Value* || *Version* ||
|| accept || fileselect function file type || {{{filename}}}, {{{savefilename}}}, {{{directory}}} or {{{newdirectory}}} || ||

== Directives ==
Some of these may have tag attribute equivalents.
|| *Name* || *Description* || *Contents* || *Mandatory* || *Version* ||
|| default || Initial selection || Data || || ||
|| variable || Shell variable name || || || ||
|| input || Data input source || Shell command || || ||
|| input file || Data input source || Filename || || ||
|| visible || Sensitive state || {{{enabled}}} or {{{disabled}}} || || ||
|| action || Execute command on default event || Shell command || || ||
|| action || Perform function on default event || _function_:_parameter_ || || ||
|| action signal="_type_" || Execute command on event || Shell command || || ||
|| action signal="_type_" || Perform function on event || _function_:_parameter_ || || ||
|| action type="_function_" || Perform function on default event || _parameter_ || || ||
|| item || Input data || || || ||
|| output file || Data output target || Filename || || ||

== Signals ==
See the [http://developer.gnome.org/gtk/2.24/GtkComboBox.html#GtkComboBox.signals GtkComboBox] widget signals.

== Functions ==
The following functions can be performed upon this widget by any widget capable of emitting signals:
|| *Type* || *Description* || *Parameter* || *Version* ||
|| command || Execute command || Shell command || ||
|| enable || Sensitise widget || Shell variable name || ||
|| disable || Desensitise widget || Shell variable name || ||
|| refresh || Reload input data || Shell variable name || ||
|| save || Save widget data || Shell variable name || ||
|| fileselect || Launch file selection dialog || Shell variable name || ||
|| clear || Remove all widget data || Shell variable name || ||
|| removeselected || Remove selected widget data || Shell variable name || ||

The following general functions can be performed by any widget capable of emitting signals:
|| *Type* || *Description* || *Parameter* || *Version* ||
|| exit || Exit dialog || A value for the EXIT shell variable || ||
|| closewindow || Close launched dialog || Launched dialog's shell variable name || ||
|| launch || Launch new dialog || New dialog's shell variable name || ||

== Notes ==
This widget was introduced in version 0.7.21.
----