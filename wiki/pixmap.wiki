#summary pixmap widget reference

= pixmap widget =
A [http://developer.gnome.org/gtk/2.24/GtkImage.html GtkImage]
----

== Definition ==
{{{
<pixmap tag_attr="value"...>
	<variable>varname</variable>
	<height>value</height>
	<width>value</width>
	<input file icon="image"></input>
	<input file stock="gtk-image"></input>
	<input file>filename</input>
	<visible>state</visible>
</pixmap>
}}}
"..." denotes acceptance of multiples of the same thing.

== Tag Attributes ==
See the [http://developer.gnome.org/gtk/2.24/GtkImage.html#GtkImage.object-hierarchy GtkImage] widget and ancestor class properties.

The following custom tag attributes are available:
None.

== Directives ==
Some of these may have tag attribute equivalents.
|| *Name* || *Description* || *Contents* || *Mandatory* || *Since* ||
|| variable || Shell variable name || || || ||
|| height || Image scaling dimension || An integer > {{{0}}} or {{{-1}}} to ignore || || 0.7.21 ||
|| width || Image scaling dimension || An integer > {{{0}}} or {{{-1}}} to ignore || || 0.7.21 ||
|| input file icon="_image_"^[1]^ || GTK theme image name  || || (0) || 0.7.21 ||
|| input file stock="_gtk-image_"^[2]^ || Stock GTK image name || {{{gtk-about}}}, {{{gtk-add}}}, ... [http://developer.gnome.org/gtk/2.24/gtk-Stock-Items.html#GTK-STOCK-ABOUT:CAPS (full list)] || (0) || ||
|| input file || Data input source || Image filename || (0) || ||
|| visible || Sensitive state || {{{enabled}}} or {{{disabled}}} || || ||

== Signals ==
None.

== Functions ==
The following functions can be performed upon this widget by any widget capable of emitting signals:
|| *Type* || *Description* || *Parameter* || *Since* ||
|| enable || Sensitise widget || Shell variable name || ||
|| disable || Desensitise widget || Shell variable name || ||
|| refresh || Reload input data || Shell variable name || 0.7.21 ||

== Notes ==
1. Theme images default to 32 and do not scale or refresh (the height or width directives can be used to adjust the size).

2. Stock images default to GTK_ICON_SIZE_DND and do not scale or refresh (the GTK+ "[http://developer.gnome.org/gtk/2.24/GtkImage.html#GtkImage--icon-size icon-size]" property can be used to adjust the size).

----