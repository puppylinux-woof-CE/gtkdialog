#summary entry widget reference

= entry widget =
A [http://developer.gnome.org/gtk/2.24/GtkEntry.html GtkEntry]
----

== Definition ==
{{{
<entry tag_attr="value"...>
	<default>text</default>
	<variable>varname</variable>
	<height>value</height>
	<width>value</width>
	<input>command</input>
	<input file>filename</input>
	<sensitive>state</sensitive>
	<action>activity</action>...
	<action signal="type">activity</action>...
	<action type="function">parameter</action>...
	<output file>filename</output>
</entry>
}}}
"..." denotes acceptance of multiples of the same thing.

== Tag Attributes ==
See the [http://developer.gnome.org/gtk/2.24/GtkEntry.html#GtkEntry.object-hierarchy GtkEntry] widget and ancestor class properties.

The following custom tag attributes are available:
|| *Name* || *Description* || *Value* || *Since* ||
|| block-function-signals || Block signal emissions from functions || {{{true}}} or {{{false}}} || 0.7.21 ||
|| fs-action || fileselect function action type || {{{file}}}, {{{newfile}}}, {{{folder}}} or {{{newfolder}}} || 0.7.21 ||
|| fs-filters || fileselect function pattern file filters || _pattern0_{{{|}}}_pattern1_{{{|}}}... || 0.7.21 ||
|| fs-filters-mime || fileselect function mime-type file filters || _mime/type0_{{{|}}}_mime/type1_{{{|}}}... || 0.7.21 ||
|| fs-folder || fileselect function current folder || || 0.7.21 ||
|| fs-title || fileselect function dialog title || || 0.7.21 ||

== Directives ==
Some of these may have tag attribute equivalents.
|| *Name* || *Description* || *Contents* || *Mandatory* || *Since* ||
|| default || Initial data || Text || || ||
|| variable || Shell variable name || || || ||
|| height || Initial minimum height || An integer > {{{0}}} or {{{-1}}} to ignore || || ||
|| width || Initial minimum width || An integer > {{{0}}} or {{{-1}}} to ignore || || ||
|| input || Data input source || Shell command || || ||
|| input file || Data input source || Filename || || 0.7.21 ||
|| sensitive || Sensitive state || {{{true}}}, {{{false}}} or {{{password}}} || || 0.7.21 ||
|| action || Execute command on default signal || Shell command || || ||
|| action || Perform function on default signal || _function_:_parameter_ || || ||
|| action signal="_type_" || Execute command on signal || Shell command || || ||
|| action signal="_type_" || Perform function on signal || _function_:_parameter_ || || ||
|| action type="_function_" || Perform function on default signal || _parameter_ || || ||
|| output file || Data output target || Filename || || ||

== Signals ==
The default signal is "changed", emitted when typing into the entry.

The "[http://developer.gnome.org/gtk/2.24/GtkEntry.html#GtkEntry-activate activate]" signal is emitted when the user activates the entry with the Enter key (since 0.7.21).

For GTK+ 2.16 and later, the "[http://developer.gnome.org/gtk/2.24/GtkEntry.html#GtkEntry-icon-press icon-press]" and "[http://developer.gnome.org/gtk/2.24/GtkEntry.html#GtkEntry-icon-release icon-release]" signals are emitted when an activatable icon is pressed or released, but these signal names will be prefixed with either "primary-" or "secondary-" to indicate the source icon (since 0.7.21).

See also the [http://developer.gnome.org/gtk/2.24/GtkEntry.html#GtkEntry.signals GtkEntry] widget and ancestor class signals.

== Functions ==
The following functions can be performed upon this widget by any widget capable of emitting signals:
|| *Type* || *Description* || *Parameter* || *Since* ||
|| enable || Sensitise widget || Shell variable name || ||
|| disable || Desensitise widget || Shell variable name || ||
|| refresh || Reload input data || Shell variable name || ||
|| save || Save widget data || Shell variable name || 0.7.21 ||
|| fileselect || Launch file selection dialog || Shell variable name || ||
|| clear || Remove all widget data || Shell variable name || ||
|| removeselected || Remove selected widget data || Shell variable name || ||

The following general functions can be performed by any widget capable of emitting signals:
|| *Type* || *Description* || *Parameter* || *Since* ||
|| command || Execute command || Shell command || ||
|| exit || Exit dialog || A value for the {{{EXIT}}} shell variable || ||
|| closewindow || Close launched dialog || Launched dialog's shell variable name || ||
|| launch || Launch new dialog || New dialog's shell variable name || ||

== Notes ==
None.
----